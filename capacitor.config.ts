import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-routing-query-string-bug',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
